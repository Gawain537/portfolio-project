name: Deploy to Azure Static Web Apps

on:
  workflow_dispatch: # Allows manual trigger from GitHub UI
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [main]

jobs:
  build_and_deploy_job:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4

      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "Prod"       # Path to your app source code
          api_location: "api"     # Path to your Azure Functions API (optional)
          output_location: "" # Path to built static assets (e.g., dist, build)
          skip_app_build: true # Set to true if you want to skip the build step